#include "main.h"
/**
 * _strspn - returns the number of bytes in the initial segment of s which consists only
             of bytes from accept
 *
 * @char *s:
 * @char *accept:
 *
 * Return: a pointer to @dest
 */
